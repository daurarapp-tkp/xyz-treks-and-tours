'use client';
import {useState} from 'react';import {supabase} from '@/lib/supabase';
export default function Contact(){const [sent,setSent]=useState(false),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);const {error}=await supabase.from('xyz_bookings').insert({customer_name:f.get('name'),email:f.get('email'),phone:f.get('phone'),country:f.get('country'),travelers:Number(f.get('travelers')||1),travel_date:f.get('date')||null,message:f.get('message')});setLoading(false);if(error)alert(error.message);else{setSent(true);e.currentTarget.reset()}}return <><section className="pageHero"><div className="container"><div className="eyebrow">Let's plan together</div><h1>Plan your trip</h1></div></section><section className="section"><div className="container"><form className="form" onSubmit={submit}><h2>Send an enquiry</h2>{sent&&<p style={{color:'green',fontWeight:700}}>Thank you! Your enquiry has been received.</p>}<div className="row"><div className="field"><label>Name *</label><input name="name" required/></div><div className="field"><label>Email</label><input type="email" name="email"/></div></div><div className="row"><div className="field"><label>Phone / WhatsApp</label><input name="phone"/></div><div className="field"><label>Country</label><input name="country"/></div></div><div className="row"><div className="field"><label>Travelers</label><input type="number" name="travelers" min="1" defaultValue="1"/></div><div className="field"><label>Preferred date</label><input type="date" name="date"/></div></div><div className="field"><label>Message</label><textarea name="message" placeholder="Tell us where you want to go, your dates and interests..."/></div><button className="btn primary" disabled={loading}>{loading?'Sending...':'Send Enquiry'}</button></form></div></section></>}
