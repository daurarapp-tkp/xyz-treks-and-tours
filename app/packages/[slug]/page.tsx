import Link from 'next/link';
export default async function Package({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const name=slug.split('-').map(s=>s[0].toUpperCase()+s.slice(1)).join(' ');return <><section className="pageHero"><div className="container"><div className="eyebrow">XYZ Treks & Tours</div><h1>{name}</h1></div></section><section className="section white"><div className="container feature"><div><span className="badge">Guided • Nepal</span><h2>{name}</h2><p className="muted" style={{lineHeight:1.9}}>This package page is connected to the Supabase package database architecture. The production version can load the complete itinerary, inclusions, exclusions, map and gallery from the admin panel.</p><ul><li>Airport pickup and trip briefing</li><li>Professional local guide</li><li>Accommodation according to itinerary</li><li>Permits and required local arrangements</li></ul><Link href={`/contact?package=${encodeURIComponent(name)}`} className="btn primary">Request Booking</Link></div><img src="https://images.unsplash.com/photo-1486911278844-a81c5267e227?auto=format&fit=crop&w=1200&q=85" alt={name}/></div></section></>}
